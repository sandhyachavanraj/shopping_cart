
<div>
  <table class="menu">
    
    <td class="btn"  > <%= link_to("Users", admin_path) %></td>
    <td class="btn" >  <%= link_to("Products", admin_products_path )%></td>
    <td class="btn" ><%= link_to("Categories", admin_categories_path)%></td>
    <td class="btn" >  <%= link_to("Cart", admin_carts_path)%> </td>
   
  </table>
  <%=yield%>
</div>
